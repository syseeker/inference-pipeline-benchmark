# Minimal runtime deps for the pipeline scaffolding.
# Framework-specific deps (vllm, sglang, tensorrt-llm, modelopt, tritonclient)
# are intentionally NOT pinned here — install via `pip install -e ".[vllm]"` etc.
pydantic>=2.6
httpx>=0.27
numpy>=1.26
pillow>=10.0
pyyaml>=6.0
rich>=13.7
typer>=0.12
openai>=1.30
